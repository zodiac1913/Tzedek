# Bookmarklet Plan

The bookmarklet should remain close to the extension experience.

Near term:

- host a shared loader and runner bundle from a public repository
- keep the bookmarklet itself small
- keep UI and audit behavior aligned with the extension

Long term outputs should include:

- a tiny loader bookmarklet string
- a hosted loader script
- a hosted runner bundle
